# FlowLens Frontend

This is a minimal Vite + React scaffold that expects a backend API mounted under `/api`.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

The UI calls `/api/ping` and `/api/linked-list/process` as examples. Implement those endpoints in the backend.
